import React from "react";

const Book = props => {
  let { book, clickHandler } = props;
  return (
    <div>
      <h2>{/*Book Title*/}</h2>
      {/* Render Book Image */}
    </div>
  );
};

export default Book;
